# Copier en deploy/.env (ignoré par git) puis remplir.
LBC_USER=nrk
# bcrypt : docker run --rm caddy:2-alpine caddy hash-password --plaintext 'mon-mot-de-passe'
LBC_HASH=
LBC_MODE=live
# Gateway OpenAI-compatible : sans elle, la cle LLM seule ne suffit pas.
LLM_BASE_URL=
LLM_MODEL=gemini-3.7-flash-high
